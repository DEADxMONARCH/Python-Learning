# tuple is ordered , mutable , heterogeneous , 
#indexed m allows suplicates 
# can be access like a[i], slice a[4:1] , update [2] = 10,
# del a[1] delet elements 
#----------------------------------------------
# Method	Meaning
#-------------------------------
# append(x)	add item at end
# insert(i,x)	insert at index
# extend(iterable)	add multiple values
# remove(x)	remove first matching element
# pop(i)	remove and return element
# clear()	empty list
# index(x)	returns index of element
# count(x)	frequency
# sort()	ascending order (changes original)
# reverse()	reverse elements
# copy()	copy list
t = 